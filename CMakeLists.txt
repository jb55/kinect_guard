CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(kinect_guard)

SET(CMAKE_MODULE_PATH cmake)
SET(FIND_PACKAGE_USE_LIB64_PATHS ON)

FIND_PACKAGE(Freenect)
FIND_PACKAGE(ZeroMQ)

INCLUDE_DIRECTORIES(
  ${Freenect_INCLUDE_DIR}
  ${ZeroMQ_INCLUDE_DIR}
)

ADD_SUBDIRECTORY(src)

